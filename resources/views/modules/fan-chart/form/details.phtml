<?php

/**
 * This file is part of the package magicsunday/webtrees-fan-chart.
 *
 * For the full copyright and license information, please read the
 * LICENSE file distributed with this source code.
 */

declare(strict_types=1);

use Fisharebest\Webtrees\I18N;
use MagicSunday\Webtrees\FanChart\Configuration;

/**
 * @var Configuration $configuration
 * @var string        $moduleName
 */
?>

<div class="row mt-3">
    <div class="col-form-label col-sm-3 wt-page-options-label">
        <?= I18N::translate('Details') ?>
    </div>
    <div class="col-sm-9 wt-page-options-value">
        <?=
            view($moduleName . '::modules/components/checkbox', [
                'name'      => 'showPlaces',
                'label'     => I18N::translate('Show places'),
                'checked'   => $configuration->getShowPlaces(),
                'unchecked' => '0',
            ])
        ?>
        <small class="ps-4 form-text text-muted form-element-description" style="box-decoration-break: clone;">
            <?= I18N::translate('Displays birth and death places in the chart arcs where space allows. For descendants with many children, places are automatically suppressed. Choose the level of detail below.') ?>
        </small>
        <div class="ps-4 mt-2">
            <?=
                view('components/select', [
                    'name'     => 'placeParts',
                    'selected' => $configuration->getPlaceParts(),
                    'options'  => $configuration->getPlacePartsList(),
                    'class'    => 'form-control-sm',
                ])
            ?>
        </div>

        <div class="mt-3">
            <?=
                view($moduleName . '::modules/components/checkbox', [
                    'name'      => 'showParentMarriageDates',
                    'label'     => I18N::translate('Show parent marriage dates'),
                    'checked'   => $configuration->getShowParentMarriageDates(),
                    'unchecked' => '0',
                ])
            ?>
            <small class="ps-4 form-text text-muted form-element-description" style="box-decoration-break: clone;">
                <?= I18N::translate('Displays the marriage date of each couple in a narrow arc between generations. When descendants are enabled, also shows the marriage date between the central person and their partners.') ?>
            </small>
        </div>

        <div class="mt-3">
            <?= view($moduleName . '::modules/fan-chart/form/date-precision', ['moduleName' => $moduleName, 'configuration' => $configuration]) ?>
        </div>
    </div>
</div>
