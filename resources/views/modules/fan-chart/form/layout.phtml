<?php

/**
 * This file is part of the package magicsunday/webtrees-fan-chart.
 *
 * For the full copyright and license information, please read the
 * LICENSE file distributed with this source code.
 */

declare(strict_types=1);

use Fisharebest\Webtrees\I18N;
use MagicSunday\Webtrees\FanChart\Configuration;

/**
 * @var Configuration $configuration
 * @var string        $moduleName
 */
?>

<div class="row">
    <div class="col-form-label col-sm-3 wt-page-options-label">
        <?= I18N::translate('Layout') ?>
    </div>
    <div class="col-sm-9 wt-page-options-value">
        <?=
            view($moduleName . '::modules/components/checkbox', [
                'name'      => 'hideEmptySegments',
                'label'     => I18N::translate('Hide empty segments'),
                'checked'   => $configuration->getHideEmptySegments(),
                'unchecked' => '0',
            ])
        ?>

        <div class="mt-3">
            <?=
                view($moduleName . '::modules/components/checkbox', [
                    'name'      => 'showNames',
                    'label'     => I18N::translate('Show names'),
                    'checked'   => $configuration->getShowNames(),
                    'unchecked' => '0',
                ])
            ?>
            <small class="ps-4 form-text text-muted form-element-description" style="box-decoration-break: clone;">
                <?= I18N::translate('Displays names and dates in the chart segments. When disabled, outer generations beyond the inner arcs are hidden.') ?>
            </small>
        </div>

        <div class="mt-3">
            <?=
                view($moduleName . '::modules/components/checkbox', [
                    'name'      => 'showImages',
                    'label'     => I18N::translate('Show images'),
                    'checked'   => $configuration->getShowImages(),
                    'unchecked' => '0',
                ])
            ?>
            <small class="ps-4 form-text text-muted form-element-description" style="box-decoration-break: clone;">
                <?= I18N::translate('Displays thumbnail images in the inner arcs and the center of the chart. Images are only shown if the arc segment is wide enough.') ?>
            </small>
        </div>

        <div class="mt-3">
            <?=
                view($moduleName . '::modules/components/checkbox', [
                    'name'      => 'showPlaces',
                    'label'     => I18N::translate('Show places'),
                    'checked'   => $configuration->getShowPlaces(),
                    'unchecked' => '0',
                ])
            ?>
            <small class="ps-4 form-text text-muted form-element-description" style="box-decoration-break: clone;">
                <?= I18N::translate('Displays birth and death places in inner generation arcs. Choose the level of detail below.') ?>
            </small>
            <div class="ps-4 mt-2">
                <?=
                    view('components/select', [
                        'name'     => 'placeParts',
                        'selected' => $configuration->getPlaceParts(),
                        'options'  => $configuration->getPlacePartsList(),
                        'class'    => 'form-control-sm',
                    ])
                ?>
            </div>
        </div>

        <div class="mt-3">
            <?=
                view($moduleName . '::modules/components/checkbox', [
                    'name'      => 'showParentMarriageDates',
                    'label'     => I18N::translate('Show parent marriage dates'),
                    'checked'   => $configuration->getShowParentMarriageDates(),
                    'unchecked' => '0',
                ])
            ?>
        </div>

        <div class="mt-3">
            <?=
                view($moduleName . '::modules/components/checkbox', [
                    'name'      => 'showFamilyColors',
                    'label'     => I18N::translate('Show family colors'),
                    'checked'   => $configuration->getShowFamilyColors(),
                    'unchecked' => '0',
                ])
            ?>
            <small class="ps-4 form-text text-muted form-element-description" style="box-decoration-break: clone;">
                <?= I18N::translate('Colors the arcs by family branch. Each couple shares the same color, derived from the chosen base colors.') ?>
            </small>
            <div class="ps-4 mt-2 d-flex align-items-center gap-3">
                <label class="d-flex align-items-center gap-1 mb-0">
                    <input type="color" id="paternalColor" name="paternalColor" value="<?= e($configuration->getPaternalColor()) ?>">
                    <?= I18N::translate('Paternal') ?>
                </label>
                <label class="d-flex align-items-center gap-1 mb-0">
                    <input type="color" id="maternalColor" name="maternalColor" value="<?= e($configuration->getMaternalColor()) ?>">
                    <?= I18N::translate('Maternal') ?>
                </label>
            </div>
        </div>
    </div>
</div>
