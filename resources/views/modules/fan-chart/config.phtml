<?php

/**
 * This file is part of the package magicsunday/webtrees-fan-chart.
 *
 * For the full copyright and license information, please read the
 * LICENSE file distributed with this source code.
 */

declare(strict_types=1);

use Fisharebest\Webtrees\Http\RequestHandlers\ControlPanel;
use Fisharebest\Webtrees\Http\RequestHandlers\ModulesAllPage;
use Fisharebest\Webtrees\I18N;
use MagicSunday\Webtrees\FanChart\Configuration;

/**
 * @var Configuration $configuration
 * @var string        $moduleName
 * @var string        $title
 * @var string        $description
 */
?>

<?php echo view('components/breadcrumbs', ['links' => [route(ControlPanel::class) => I18N::translate('Control panel'), route(ModulesAllPage::class) => I18N::translate('Modules'), $title]]); ?>

<h1><?php echo $title; ?></h1>
<p><?php echo $description; ?></p>

<form method="post">
    <div class="mb-3">
        <?php echo view($moduleName . '::modules/fan-chart/form/fan-size', ['moduleName' => $moduleName, 'configuration' => $configuration]); ?>
    </div>
    <div class="mb-3">
        <?php echo view($moduleName . '::modules/fan-chart/form/generations', ['moduleName' => $moduleName, 'configuration' => $configuration]); ?>
    </div>
    <div class="mb-3">
        <?php echo view($moduleName . '::modules/fan-chart/form/display', ['moduleName' => $moduleName, 'configuration' => $configuration]); ?>
    </div>
    <div class="mb-3">
        <?php echo view($moduleName . '::modules/fan-chart/form/details', ['moduleName' => $moduleName, 'configuration' => $configuration]); ?>
    </div>
    <div class="mb-3">
        <?php echo view($moduleName . '::modules/fan-chart/form/colors', ['moduleName' => $moduleName, 'configuration' => $configuration]); ?>
    </div>
    <?php echo view($moduleName . '::modules/fan-chart/form/appearance', ['moduleName' => $moduleName, 'configuration' => $configuration]); ?>

    <div class="mb-3">
        <div class="row">
            <div class="col-form-label col-sm-3 wt-page-options-label">
                <?php echo I18N::translate('General'); ?>
            </div>
            <div class="col-sm-9 wt-page-options-value">
                <?php echo view($moduleName . '::modules/components/checkbox', [
                    'name'      => 'hideSvgExport',
                    'label'     => I18N::translate('Hide the SVG export button'),
                    'checked'   => $configuration->getHideSvgExport(),
                    'unchecked' => '0',
                ]);
?>

                <div class="mt-3">
                    <?php echo view($moduleName . '::modules/components/checkbox', [
                        'name'      => 'hidePngExport',
                        'label'     => I18N::translate('Hide the PNG export button'),
                        'checked'   => $configuration->getHidePngExport(),
                        'unchecked' => '0',
                    ]);
?>
                </div>

                <div class="mt-3">
                    <label for="nameAbbreviation" class="form-label">
                        <?php echo I18N::translate('Name abbreviation when space is tight'); ?>
                    </label>
                    <?php echo view('components/select', [
                        'name'     => 'nameAbbreviation',
                        'id'       => 'nameAbbreviation',
                        'selected' => $configuration->getNameAbbreviation(),
                        'options'  => $configuration->getNameAbbreviationList(),
                        'class'    => 'form-control-sm',
                    ]);
?>
                    <small class="form-text text-muted form-element-description" style="box-decoration-break: clone;">
                        <?php echo I18N::translate('Controls which name part shrinks first when an arc is too narrow for the full name. "Automatic" uses the tree\'s surname tradition (Icelandic abbreviates surnames; everything else abbreviates given names).'); ?>
                    </small>
                </div>

                <div class="mt-3">
                    <label for="<?php echo e(Configuration::PLACE_FORMAT_PARAM); ?>" class="form-label">
                        <?php echo I18N::translate('Place detail'); ?>
                    </label>
                    <?php echo view('components/select', [
                        'name'     => Configuration::PLACE_FORMAT_PARAM,
                        'id'       => Configuration::PLACE_FORMAT_PARAM,
                        'selected' => $configuration->getPlaceFormatChoice()->value,
                        'options'  => $configuration->getPlaceFormatList(),
                        'class'    => 'form-control-sm',
                    ]);
?>
                    <small class="form-text text-muted form-element-description" style="box-decoration-break: clone;">
                        <?php echo I18N::translate('How much of each place is shown in chart arcs — the full name, only the lowest levels, or the place combined with its country as a name or ISO code. Users toggle place visibility on/off in the chart form.'); ?>
                    </small>
                </div>

                <div class="mt-3">
                    <label for="detailedDateGenerations" class="form-label">
                        <?php echo I18N::translate('Date precision'); ?>
                    </label>
                    <?php echo view('components/select', [
                        'name'     => 'detailedDateGenerations',
                        'id'       => 'detailedDateGenerations',
                        'selected' => $configuration->getDetailedDateGenerations(),
                        'options'  => $configuration->getDetailedDateGenerationsList(),
                        'class'    => 'form-control-sm',
                    ]);
?>
                    <small class="form-text text-muted form-element-description" style="box-decoration-break: clone;">
                        <?php echo I18N::translate('Show full dates (birth, marriage, death) for early generations. Later generations show year only. Marriage dates are hidden from generation 9. Descendants with narrow arcs automatically use a compact year-only format.'); ?>
                    </small>
                </div>
            </div>
        </div>
    </div>

    <div class="row mb-3">
        <div class="offset-sm-3 col-sm-9">
            <button type="submit" class="btn btn-primary">
                <?php echo I18N::translate('save'); ?>
            </button>

            <a href="<?php echo route(ControlPanel::class); ?>" class="btn btn-secondary">
                <?php echo view('icons/cancel'); ?>
                <?php echo I18N::translate('cancel'); ?>
            </a>
        </div>
    </div>

    <?php echo csrf_field(); ?>
</form>
