name: Auto-merge dependency PRs

on:
    pull_request:

permissions: {}

jobs:
    auto-merge:
        uses: magicsunday/.github/.github/workflows/auto-merge-deps.yml@main
        permissions:
            contents: write
            pull-requests: write
